---
interface Props {
  title: string;
  description: string;
}
const { title, description } = Astro.props;
---
<div class="caps-header">
  <h3 class="caps-main-title">{title}</h3>
  <p class="caps-main-description">{description}</p>
</div>
