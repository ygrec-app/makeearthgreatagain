# Cloudflare Pages reads this file at deploy time for path-level redirects
# inside the project. Cross-hostname redirects (e.g. www → apex) are handled
# by a Cloudflare Redirect Rule on the zone, not here — see CLAUDE.md.
